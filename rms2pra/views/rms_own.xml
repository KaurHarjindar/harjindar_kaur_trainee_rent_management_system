<odoo>
    <record model="ir.ui.view" id="ownerForm">
        <field name="name">training.tree</field>
        <field name="model">training.owner</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="owner_name" />
                        <field name="RoomDetail" />
                        <field name="RoomType" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- <record model="ir.ui.view" id="owner_search_view">
            <field name="name">owner.search</field>
            <field name="model">training.owner</field>
            <field name="arch" type="xml">
                <search>
                    <field name="owner_name"/>
                </search>
            </field>
    </record> -->

   <!--  <record model="ir.ui.view" id="tenanteview">
        <field name="name">training.tree</field>
        <field name="model">training.tenant</field>
        <field name="arch" type="xml">
            <tree string="tree view" decoration-info="Occupation=='Study'">
                <field name="tenant_id" />
                <field name="dob" />
                <field name="gender" />
                <field name="Marital_status" />
                <field name="Home_address" />
                <field name="Occupation" />
                <field name="Joining_date"/>
                <field name="tenant_name" />
                <field name="phone" />
                <field name="email" />
                <field name="Religion" />   
                <field name="Father_name" />
                <field name="Fphone" />
                <field name="room_ids" />
            </tree>
        </field>
    </record> -->

    <record id="marse_marse_action3" model="ir.actions.act_window">
        <field name="name">training tenant</field>
        <field name="res_model">training.owner</field>
        <field name="view_mode">tree,form</field>
    </record>

     <menuitem id="tenants_owner_root" name="Owner"/>
    <menuitem id="tenants_manage" name="ownerRegi" parent="tenants_owner_root" action="marse_marse_action3"/>
</odoo>